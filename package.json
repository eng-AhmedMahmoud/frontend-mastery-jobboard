{
  "name": "frontend-mastery-jobboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Frontend Mastery — the job board built across the program. One repo, three branches per stage.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "strip": "node scripts/strip.mjs",
    "stage:publish": "node scripts/publish-stage.mjs",
    "stage:verify": "node scripts/verify-stage.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "packageManager": "pnpm@11.10.0"
}
