/**
 * Applications — applying to a role and tracking what happened.
 *
 * Module 8 owns it; module 9 gives it a server action and a real database.
 */
export { useApplications } from './hooks/use-applications'
export { submitApplication } from './api/applications-api'
